apply(plugin: 'java')
apply(plugin: 'idea')
apply(plugin: 'application')

repositories {
  jcenter()
}

sourceSets {
  main {
    java {
      srcDir 'src/java'
    }
  }
}

mainClassName = "server.Server"